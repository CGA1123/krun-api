// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: vminit/v1/vminit.proto

package vminitv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/CGA1123/krun-api/gen/vminit/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// VMInitServiceName is the fully-qualified name of the VMInitService service.
	VMInitServiceName = "vminit.v1.VMInitService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// VMInitServiceShutdownProcedure is the fully-qualified name of the VMInitService's Shutdown RPC.
	VMInitServiceShutdownProcedure = "/vminit.v1.VMInitService/Shutdown"
)

// VMInitServiceClient is a client for the vminit.v1.VMInitService service.
type VMInitServiceClient interface {
	Shutdown(context.Context, *connect.Request[v1.ShutdownRequest]) (*connect.Response[v1.ShutdownResponse], error)
}

// NewVMInitServiceClient constructs a client for the vminit.v1.VMInitService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewVMInitServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) VMInitServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	vMInitServiceMethods := v1.File_vminit_v1_vminit_proto.Services().ByName("VMInitService").Methods()
	return &vMInitServiceClient{
		shutdown: connect.NewClient[v1.ShutdownRequest, v1.ShutdownResponse](
			httpClient,
			baseURL+VMInitServiceShutdownProcedure,
			connect.WithSchema(vMInitServiceMethods.ByName("Shutdown")),
			connect.WithClientOptions(opts...),
		),
	}
}

// vMInitServiceClient implements VMInitServiceClient.
type vMInitServiceClient struct {
	shutdown *connect.Client[v1.ShutdownRequest, v1.ShutdownResponse]
}

// Shutdown calls vminit.v1.VMInitService.Shutdown.
func (c *vMInitServiceClient) Shutdown(ctx context.Context, req *connect.Request[v1.ShutdownRequest]) (*connect.Response[v1.ShutdownResponse], error) {
	return c.shutdown.CallUnary(ctx, req)
}

// VMInitServiceHandler is an implementation of the vminit.v1.VMInitService service.
type VMInitServiceHandler interface {
	Shutdown(context.Context, *connect.Request[v1.ShutdownRequest]) (*connect.Response[v1.ShutdownResponse], error)
}

// NewVMInitServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewVMInitServiceHandler(svc VMInitServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	vMInitServiceMethods := v1.File_vminit_v1_vminit_proto.Services().ByName("VMInitService").Methods()
	vMInitServiceShutdownHandler := connect.NewUnaryHandler(
		VMInitServiceShutdownProcedure,
		svc.Shutdown,
		connect.WithSchema(vMInitServiceMethods.ByName("Shutdown")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vminit.v1.VMInitService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case VMInitServiceShutdownProcedure:
			vMInitServiceShutdownHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedVMInitServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedVMInitServiceHandler struct{}

func (UnimplementedVMInitServiceHandler) Shutdown(context.Context, *connect.Request[v1.ShutdownRequest]) (*connect.Response[v1.ShutdownResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vminit.v1.VMInitService.Shutdown is not implemented"))
}
